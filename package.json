{
  "name": "vetements-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  }
}
